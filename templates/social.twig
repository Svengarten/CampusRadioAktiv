<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Social Media - Campus Radio Aktiv</title>
    <link rel="stylesheet" href="assets/css/custom.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body>

<header class="d-flex py-3 px-3">
    <div class="logo">
        <img src="assets/img/CRA_Logo.svg" alt="Campus Radio Aktiv Logo">
    </div>
    <nav id="main-nav">
        <ul class="nav col-12 mb-2 justify-content-center">
            <li><a href="social.php">Startseite</a></li>
            <li><a href="sendungen.php">Sendungen</a></li>
            <li><a href="#">Beiträge</a></li>
            <li><a href="#">Über uns</a></li>
            <li><a href="#">Kontakt</a></li>
        </ul>
    </nav>
    <div class="search-bar col-md-3">
        <input type="text" placeholder="Suche...">
    </div>
</header>

<main>
    <div class="album py-5">
        <div class="container">
            <div class="row g-3">
                <div class="blog-posts">
                    <div class="row row-cols-1 row-cols-sm-1 row-cols-md-2 row-cols-lg-3 g-3 px-2">
                        {% for post in posts %}
                        <div class="col">
                            <div class="card h-100">
                                <div class="card-body">
                                    <div class="sendung-post-header">
                                        <img src="{{ post.avatar }}" alt="User Avatar" class="avatar">
                                        <h5 class="sendung-title">{{ post.user_name }}</h5>
                                    </div>
                                    <p class="text" id="text-{{ loop.index }}">
                                        {{ post.post_text | slice(0, 200) }}
                                        {% if post.post_text | length > 200 %}
                                        <span class="hidden-text">{{ post.post_text | slice(200) }}</span>
                                        <a href="javascript:void(0)" onclick="toggleText({{ loop.index }})" class="toggle-button" id="toggle-button-{{ loop.index }}">Mehr anzeigen</a>
                                        {% endif %}
                                    </p>
                                </div>
                                <div class="card-footer">
                                    <div class="likes">{{ post.likes }} Likes</div>
                                </div>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                </div>
                <aside class="info-area">
                    <div class="person">
                        <img src="assets/img/presenter1.jpg" alt="Presenter Image">
                        <div class="person-info">
                            <h5>Max Mustermann</h5>
                            <p>Moderator</p>
                        </div>
                    </div>
                    <div class="contact-form">
                        <form action="#">
                            <textarea placeholder="Deine Nachricht..."></textarea>
                            <input type="submit" value="Senden">
                        </form>
                    </div>
                </aside>
            </div>
        </div>
    </div>
</main>

<footer>
    <p>&copy; 2024 Campus Radio Aktiv</p>
</footer>

<script>
    function toggleText(index) {
        var textElement = document.getElementById('text-' + index);
        textElement.classList.toggle('expanded');
        var button = document.getElementById('toggle-button-' + index);
        if (textElement.classList.contains('expanded')) {
            button.textContent = 'Weniger anzeigen';
        } else {
            button.textContent = 'Mehr anzeigen';
        }
    }
</script>

</body>
</html>
